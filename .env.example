# Credenciales del servidor (Neubox / cPanel). NO subir a git.
# Copie como `.env` en la raíz del proyecto en el hosting, o use config.local.php.

HAY_DB_HOST=localhost
HAY_DB_NAME=cncmedum_hay_system
HAY_DB_USER=cncmedum_tovar
HAY_DB_PASS=PONER_AQUI_LA_CLAVE_DE_CPANEL

# Opcional:
# HAY_WEB_ROOT=/hay/
# OPENROUTER_API_KEY=
# GEMINI_API_KEY=
