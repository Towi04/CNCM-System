<?php
/**
 * Copie como config.local.php en la raíz de HAY (no subir a producción con datos locales).
 * En XAMPP suele bastar definir la base de datos; HAY_WEB_ROOT solo si las rutas CSS/JS fallan.
 */

/** XAMPP: carpeta en htdocs (ej. C:\xampp\htdocs\hay → http://localhost/hay/) */
// define('HAY_WEB_ROOT', '/hay/');

/** Producción en subdominio (archivos en raíz): deje vacío o comente la línea. */
// define('HAY_WEB_ROOT', '');

/**
 * MySQL — en cPanel use el usuario COMPLETO (ej. cncmedum_tovar), no solo "cncmedum".
 * Las cuatro líneas son obligatorias en producción.
 */
define('HAY_DB_HOST', 'localhost');
define('HAY_DB_NAME', 'cncmedum_hay_system');
define('HAY_DB_USER', 'cncmedum_tovar');
define('HAY_DB_PASS', 'PONER_AQUI_LA_CLAVE_DE_CPANEL');

/** Obligatorio en producción si la app está en subcarpeta (ej. https://cncm.edu.mx/hay/). En subdominio raíz déjelo comentado. */
// define('HAY_WEB_ROOT', '/hay/');

/** Correo en local: use config.mail.php con MAIL_DRIVER=php o deje sin enviar */
// define('MAIL_DRIVER', 'php');

/** FingerJet (opcional, misma PC que el lector) */
// define('HAY_FINGERJET_ENABLED', true);
// define('HAY_FINGERJET_MODE', 'auto');
// define('HAY_FINGERJET_MATCHER_URL', 'http://127.0.0.1:8765');
// define('HAY_FINGERJET_MATCHER_KEY', 'clave-local-de-prueba');

/**
 * Migraciones de tablas: por defecto NO corren en login (rápido).
 * Para forzar una pasada completa una sola vez en producción:
 *   define('HAY_RUN_SCHEMA_BOOTSTRAP', true);
 * Luego comente o elimine esa línea.
 * NO ponga HAY_FORCE_SCHEMA_BOOTSTRAP al final de config.php (no tiene efecto).
 */

/** Legado / Moodle (opcional en desarrollo) */
// define('MOODLE_URL', 'https://www.cncm.edu.mx/courses');
// define('MOODLE_TOKEN', '');

/** Sugerencias de planeación con IA */
// Proveedor: 'openrouter' | 'gemini' | 'auto' (auto = OpenRouter si hay clave, si no Gemini)
// define('HAY_AI_PROVIDER', 'openrouter');

/** OpenRouter — https://openrouter.ai/keys (acceso a OpenAI, Claude, etc.) */
// define('OPENROUTER_API_KEY', 'sk-or-v1-...');
// define('OPENROUTER_MODEL', 'openai/gpt-4o');
// define('OPENROUTER_SITE_URL', 'https://sistema.cncm.edu.mx');
// define('OPENROUTER_SITE_NAME', 'Portal CNCM');

/** Google Gemini (alternativa) */
// define('GEMINI_API_KEY', 'su-clave-de-google-ai-studio');

/** Tutor Académico IA */
// define('OPENROUTER_TUTOR_MODEL', 'openai/gpt-4o');
// define('OPENROUTER_TUTOR_MAX_TOKENS', 1800);
// define('TUTOR_MOODLE_API_KEY', 'clave-secreta-para-api-moodle');

/**
 * Si el token no lista cursos (permisos), mapa shortname/idnumber → ID interno mdl_course.id.
 * Ej. Exam idnumber=4 → id interno 168:
 */
// define('MOODLE_COURSE_BY_SHORTNAME', ['Exam' => 168]);
// define('MOODLE_COURSE_BY_IDNUMBER', ['4' => 168]);
// define('MOODLE_USERNAME_PREFIX', ''); // vacío = username 14580; 'nc' → nc14580 si su Moodle lo requiere
// define('MOODLE_USER_ID_BY_CONTROL', ['14580' => 2923]); // fallback si la API no encuentra al usuario

/** Google Workspace — cuenta de servicio + delegación (Admin SDK) */
// define('GOOGLE_KEY_FILE', __DIR__ . '/google_key.json');
// define('GOOGLE_ADMIN_EMAIL', 'englishcoordinator@cncm.edu.mx');
// define('GOOGLE_WORKSPACE_DOMAIN', 'cncm.edu.mx');

/** Clave opcional para php/moodle_test.php?paso=crear&key=... sin sesión (solo servidor / ops). */
// define('MOODLE_DIAG_KEY', 'cambie-esta-clave-larga-y-secreta');
// Si vendor está en otra ruta en el hosting:
// define('HAY_COMPOSER_AUTOLOAD', __DIR__ . '/vendor/autoload.php');
// define('LEGACY_DB_HOST', 'localhost');
// define('LEGACY_DB_NAME', 'cncmedum_legado');
// define('LEGACY_DB_USER', 'root');
// define('LEGACY_DB_PASS', '');
